spring:
    profiles:
        active: prod
    banner:
        location: classpath:banner.txt
#    main:
#        allow-bean-definition-overriding: true
logging:
    pattern:
        console: "%d %-5level %logger : %msg%n"
    level:
        org.springframework: ERROR

    file:
        name: /tmp/matmonkey.log
